# 🚀 MWS Content Analyzer — Умный реестр контента

**Интеллектуальная система агрегации и анализа контента для экосистемы МТС.**

Решение объединяет сбор данных из 5 популярных платформ (Telegram, VK, YouTube, Rutube, Habr), сохраняет их в **MWS Tables** и использует **LLM** для анализа тональности, саммаризации и ответов на вопросы через чат-бота.

---

## 📋 Оглавление
1. [Функциональность](#-функциональность)
2. [Стек технологий](#-стек-технологий)
3. [Структура проекта](#-структура-проекта)
4. [Предварительные требования](#-предварительные-требования)
5. [Установка и запуск](#-установка-и-запуск)
    - [Бэкенд (Python)](#1-запуск-бэкенда)
    - [Фронтенд (React)](#2-запуск-фронтенда)
6. [Переменные окружения (.env)](#-переменные-окружения-env)
7. [API и Документация](#-api-и-документация)

---

## 🌟 Функциональность

*   **Мультиканальный парсинг:** Автоматический сбор постов, видео и статей из Telegram, VK, YouTube, Rutube и Habr.
*   **Low-Code управление:** Список каналов для мониторинга управляется напрямую через таблицу в MWS Tables (без изменения кода).
*   **AI-аналитика (LLM):**
    *   Определение тональности (Positive/Negative/Neutral).
    *   Генерация краткого содержания (Summary).
*   **RAG Чат-бот:** Telegram-бот, который отвечает на вопросы по базе данных контента (например, *"Какой пост на этой неделе самый популярный?"*).
*   **Веб-дашборд:** Визуализация статистики и метрик эффективности.
*   **Экспорт:** Выгрузка аналитики в CSV.

---

## 🛠 Стек технологий

*   **Бэкенд:** Python 3.10+, FastAPI, Uvicorn.
*   **Сбор данных:** Telethon, Vk_api, Google API Client, Requests (BeautifulSoup).
*   **AI/ML:** OpenRouter API (Llama 3.3 70B Instruct).
*   **База данных:** MWS Tables (через API).
*   **Фронтенд:** React.js, Ant Design, Recharts.
*   **Интерфейс бота:** Aiogram 3.x.

---

## mb Структура проекта

```text
mws-content-analyzer/
├── backend/                  # Серверная часть (Python)
│   ├── main.py               # Основной файл приложения (API + Scrapers + Bot)
│   ├── requirements.txt      # Зависимости Python
│   └── .env                  # Ключи API и настройки (создается вручную)
│
├── frontend/                 # Клиентская часть (React)
│   ├── public/
│   ├── src/
│   │   ├── App.js            # Основной компонент
│   │   └── ...
│   ├── package.json          # Зависимости Node.js
│   └── nginx.conf            # Конфиг для деплоя (опционально)
│
├── docker-compose.yml        # (Опционально) Для запуска в контейнерах
└── README.md                 # Документация
```

---

## ⚙️ Предварительные требования

Перед запуском убедитесь, что у вас установлены:
*   **Python** (версия 3.10 или выше)
*   **Node.js** (версия 16 или выше) и **npm**

---

## 🚀 Установка и запуск

### 1. Запуск Бэкенда

1.  Перейдите в папку бэкенда:
    ```bash
    cd backend
    ```

2.  Создайте и активируйте виртуальное окружение:
    *   **Windows:**
        ```bash
        python -m venv venv
        venv\Scripts\activate
        ```
    *   **macOS / Linux:**
        ```bash
        python3 -m venv venv
        source venv/bin/activate
        ```

3.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```

4.  Настройте переменные окружения (см. раздел [.env](#-переменные-окружения-env) ниже).

5.  Запустите сервер:
    ```bash
    python main.py
    ```
    *Успешный запуск:* В консоли появится сообщение `🚀 SYSTEM ONLINE: API + BOT + SCRAPERS`.

---

### 2. Запуск Фронтенда

1.  Откройте **новый терминал** и перейдите в папку фронтенда:
    ```bash
    cd frontend
    ```

2.  Установите зависимости:
    ```bash
    npm install
    ```

3.  Запустите проект в режиме разработки:
    ```bash
    npm start
    ```

Приложение автоматически откроется в браузере по адресу [http://localhost:3000](http://localhost:3000).

---

## 🔑 Переменные окружения (.env)

Создайте файл `.env` в папке `backend/` и заполните его вашими данными:

```env
# --- MWS TABLES (База данных) ---
MWS_TOKEN=ваш_токен_mws
MWS_TABLE_ID=id_таблицы_с_постами
MWS_VIEW_ID=id_вида_таблицы_постов
MWS_CHANNELS_TABLE_ID=id_таблицы_настроек_каналов
MWS_CHANNELS_VIEW_ID=id_вида_настроек

# --- AI (OpenRouter) ---
OPENROUTER_API_KEY=sk-or-v1-...

# --- TELEGRAM ---
# Получить на my.telegram.org
TG_API_ID=123456
TG_API_HASH=ваша_хеш_сумма
# Токен бота от @BotFather
TG_BOT_TOKEN=123:ABC...

# --- YOUTUBE (Google Cloud Console) ---
YOUTUBE_API_KEY=AIza...

# --- VK (Service Key) ---
VK_ACCESS_TOKEN=ваш_сервисный_ключ_vk
VK_GROUP_DOMAIN=mts  # Дефолтная группа (опционально)

# --- RUTUBE & HABR ---
RUTUBE_CHANNEL_NAME=mts
HABR_TARGET_COMPANIES=mts_ai,telegram,vk
```

---

## 📖 API и Документация

После запуска бэкенда, интерактивная документация API (Swagger UI) доступна по адресу:
👉 **http://localhost:8000/docs**

**Основные эндпоинты:**
*   `GET /api/stats/overview` — Общая статистика.
*   `GET /api/data` — Получение списка контента с фильтрами.
*   `POST /chat` — Запрос к AI-ассистенту.
*   `POST /api/refresh` — Принудительное обновление данных из источников.
*   `GET /api/export/csv` — Скачивание отчета.

---

*Решение разработано специально для хакатона MWS.*
